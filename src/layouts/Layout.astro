---
import { ViewTransitions } from "astro:transitions"
import "aos/dist/aos.css"
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="En Geriatric Care ofrecemos servicios de enfermería y cuidado personalizado a domicilio. Nuestro equipo de enfermeras y cuidadoras altamente capacitadas brinda atención compasiva y profesional a personas de todas las edades y necesidades. Contacta con nosotros para recibir cuidados de calidad en la comodidad de tu hogar." />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon-dark.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Geriatric Care</title>
		<ViewTransitions/>
		<!-- <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet"> -->
	</head>
	<body>
		<slot />

		<style is:global>
			html {
        scroll-behavior: smooth;
      }

			body {
        overscroll-behavior: none;
      }

			@media (prefers-reduced-motion: reduce) {
        html {
          scroll-behavior: auto;
        }
      }

			.floating { 
					animation-name: floating; 
					animation-duration: 3s; 
					animation-iteration-count: infinite; 
					animation-timing-function: ease-in-out;
			}
			
			@keyframes floating { 
					0% { transform: translate(0, 0px); } 
					50% { transform: translate(0, 8px); } 
					100% { transform: translate(0, -0px); }  
			}
		
			.floating-4 { 
					animation-name: floating; 
					animation-duration: 4s; 
					animation-iteration-count: infinite; 
					animation-timing-function: ease-in-out;
			} 
		
			@keyframes floating-4 { 
					0% { transform: translate(0, 0px); } 
					50% { transform: translate(0, 8px); } 
					100% { transform: translate(0, -0px); }  
			}
		
			.dark #nurse {
				mask-image: linear-gradient(
					to bottom,
					black 85%,
					transparent 92%
				);
			}
		</style>

		<script is:inline src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/1.8.0/flowbite.min.js"></script>
		<!-- <script is:inline src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
		<script is:inline>
			AOS.init()
		</script> -->
		<script>
			import { aosInit } from "../js/aos"

			aosInit()
		</script>
	</body>
</html>